# server-next (casfa-next) — copy to .env and set values.
# Default instance: config in cell.yaml. Symbiont: cell.symbiont.yaml; set CLOUDFLARE_API_TOKEN when deploying -i symbiont.
# CELL_* are injected by cell-cli; do not set.

# --- Casfa-Dev Tunnel (可选) ---
# 多服务子域名开发时：PORT_BASE 见根目录 .env.casfa-dev.example，并设置 SSO_BASE_URL / CELL_BASE_URL。
# 详见 docs/cloudflare-tunnel-dev.md 与 docs/plans/2026-03-11-casfa-dev-tunnel-design.md
