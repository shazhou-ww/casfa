# server-next (casfa-next) — copy to .env and set values.
# Every !Env in cell.yaml must be set here (no optional). CELL_* are injected by cell-cli; do not set.
# For local overrides, copy .env.local.example to .env.local.
# Tables: realms, grants (managed by Cell). See cell.yaml params.

# =============================================================================
# Required (!Env in cell.yaml)
# =============================================================================
LOG_LEVEL=info
SSO_BASE_URL=https://auth.casfa.shazhou.me
