{
  "name": "@casfa/image-workshop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/stdio.ts",
    "typecheck": "tsc --noEmit",
    "offline": "bun run scripts/print-mcp-url.ts && serverless offline --stage dev",
    "deploy": "serverless deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "hono": "^4.6.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "serverless": "^4.0.0",
    "serverless-offline": "^14.0.0",
    "typescript": "^5.8.3"
  }
}
