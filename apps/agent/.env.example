# Agent cell — copy to .env and set values.
# CELL_* are injected by cell-cli; do not set.

# --- Casfa-Dev Tunnel (可选) ---
# 多服务子域名开发时：PORT_BASE 见根目录 .env.casfa-dev.example。
# 用 tunnel 时无需设 SSO_BASE_URL（后端按 CELL_BASE_URL 推导）；全部走 localhost 时在 .env.local 里设 SSO_BASE_URL=http://localhost:7100。
# 详见 docs/cloudflare-tunnel-dev.md
