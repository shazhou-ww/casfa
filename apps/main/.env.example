# Optional: AWS profile for deploy (default: default)
# AWS_PROFILE=your-profile

# Stack/cell params that use !Env are resolved from .env; add vars as needed.
# Example: COGNITO_USER_POOL_ID=us-east-1_xxx
