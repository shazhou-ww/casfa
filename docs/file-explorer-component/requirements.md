# CASFA File Explorer 组件需求文档

## 1. 概述

### 1.1 目标

构建一个自包含的 React 文件浏览器组件 `<CasfaExplorer />`，**仅需传入一个已初始化的 `CasfaClient` 即可工作**。组件封装所有文件系统操作逻辑，基于 MUI 实现 UI，可跨项目复用（发布为独立 npm 包 `@casfa/explorer`）。

### 1.2 设计原则

- **最小 props**: 核心只需一个 `CasfaClient` 实例。Client 已封装 serverUrl、delegate、token 管理，组件不关心认证细节
- **Backend agnostic**: 通过 `@casfa/client` SDK 与任意 CASFA server 通信
- **权限感知**: 根据 delegate 的权限（scope / canUpload / canManageDepot）动态隐藏/禁用 UI 元素
- **懒加载**: 目录按需展开，不预加载整棵树
- **可组合**: 核心逻辑与 UI 分离，支持 headless 使用或自定义渲染

### 1.3 发布形态

```
packages/explorer/
├── src/
│   ├── core/           # headless 状态管理 & API 层
│   ├── components/     # MUI UI 组件
│   ├── hooks/          # React hooks
│   ├── types.ts        # 公共类型
│   └── index.ts        # 入口
├── package.json        # peerDeps: react, @mui/material, @casfa/client
└── tsconfig.json
```

---

## 2. 组件接口

### 2.1 最小用法

```tsx
import { createClient } from "@casfa/client";
import { CasfaExplorer } from "@casfa/explorer";

// 宿主应用负责创建和管理 client（含 token 刷新）
const client = createClient({
  serverUrl: "https://casfa.example.com",
  accessToken: "AT_base64...",
  refreshToken: "RT_base64...",
});

// 最简：只传 client，组件内部显示 depot 选择器
<CasfaExplorer client={client} />

// 指定 depot：跳过选择器，直接进入文件浏览
<CasfaExplorer client={client} depotId="dpt_XXXX" />
```

### 2.2 完整 Props

```ts
interface CasfaExplorerProps {
  // ── 连接（唯一必填） ──
  client: CasfaClient;

  // ── Depot ──
  depotId?: string;                // 指定 depot。省略时组件显示内置 depot 选择器
  onDepotChange?: (depotId: string) => void;  // depot 切换回调

  // ── 初始状态 ──
  initialPath?: string;            // 初始打开的目录路径，默认 ""（根目录）
  initialLayout?: "list" | "grid"; // 默认视图模式

  // ── 尺寸 ──
  height?: string | number;        // 默认 "100%"
  width?: string | number;         // 默认 "100%"

  // ── 扩展点 ──
  extraContextMenuItems?: ExplorerMenuItem[];   // 追加自定义右键菜单项
  extraToolbarItems?: ExplorerToolbarItem[];     // 追加自定义工具栏按钮

  // ── 事件回调 ──
  onNavigate?: (path: string) => void;
  onSelect?: (items: ExplorerItem[]) => void;
  onFileOpen?: (item: ExplorerItem) => void;
  onError?: (error: ExplorerError) => void;
  onRootChange?: (newRoot: string) => void;

  // ── 自定义渲染 ──
  renderFilePreview?: (item: ExplorerItem) => React.ReactNode;
  renderEmptyState?: () => React.ReactNode;
  renderBreadcrumb?: (segments: PathSegment[]) => React.ReactNode;
  renderNodeIcon?: (item: ExplorerItem) => React.ReactNode;

  // ── 主题 ──
  sx?: SxProps<Theme>;

  // ── 国际化（UI 文案覆盖） ──
  messages?: Partial<ExplorerMessages>;
}
```

### 2.3 权限模型

**组件不接受前端权限覆盖 prop。** 权限完全由 server 端的 delegate 属性决定：

- `scope`（节点 hash）→ 决定可浏览/可读的子树范围
- `canUpload`（boolean）→ 决定所有写操作（创建、上传、重命名、移动、复制、删除）
- `canManageDepot`（boolean）→ 决定 depot 级别操作（创建/删除 depot、commit）

组件在首次加载时通过 client 查询当前 delegate 信息，据此动态显示/隐藏 UI 元素。写操作被 server 拒绝（403）时，组件更新本地权限状态并给出提示。

> **理由**：前端收窄权限无意义（server 本来就会拒绝）；前端放大权限更无意义（server 会 403）。权限是 server 端事实，不应由 prop 配置。

### 2.4 国际化 / UI 文案

`messages` prop 用于覆盖组件内所有 UI 文案，支持完整的 i18n 场景：

```ts
interface ExplorerMessages {
  // ── 工具栏 ──
  createFolder: string;        // "新建文件夹"
  upload: string;              // "上传"
  download: string;            // "下载"
  delete: string;              // "删除"
  rename: string;              // "重命名"
  refresh: string;             // "刷新"
  search: string;              // "搜索文件..."

  // ── 右键菜单 ──
  open: string;                // "打开"
  cut: string;                 // "剪切"
  copy: string;                // "复制"
  paste: string;               // "粘贴"
  copyCasUri: string;          // "复制 CAS URI"
  properties: string;          // "属性"

  // ── 对话框 ──
  confirmDeleteTitle: string;  // "确认删除"
  confirmDeleteBody: string;   // "确定要删除 {count} 个项目吗？此操作不可撤销。"
  newFolderTitle: string;      // "新建文件夹"
  newFolderPlaceholder: string;// "文件夹名称"
  renameTitle: string;         // "重命名"
  conflictTitle: string;       // "名称冲突"
  conflictOverwrite: string;   // "覆盖"
  conflictRename: string;      // "重命名"
  conflictSkip: string;        // "跳过"

  // ── 状态提示 ──
  emptyFolder: string;         // "此文件夹为空"
  dropFilesHere: string;       // "拖拽文件到此处上传"
  uploadSuccess: string;       // "已上传 {count} 个文件"
  deleteSuccess: string;       // "已删除 {count} 个项目"
  networkError: string;        // "网络不可用，请检查连接"
  permissionDenied: string;    // "权限不足"
  fileTooLarge: string;        // "文件过大（最大 4MB）"
  authExpired: string;         // "认证已过期，请重新登录"

  // ── Depot 选择器 ──
  selectDepot: string;         // "选择仓库"
  noDepots: string;            // "暂无可用仓库"
}
```

组件内置 `en-US` 和 `zh-CN` 两套默认文案。`messages` 中传入的条目会 merge 覆盖默认值。

---

## 3. 用例

### 3.1 浏览与导航

| # | 用例 | 描述 |
|---|------|------|
| B-1 | **目录浏览** | 打开一个目录，显示其子项（文件 + 子文件夹），按名称排序，文件夹在前 |
| B-2 | **懒加载展开** | 点击文件夹时按需调用 `fs/ls`，不预加载整棵树 |
| B-3 | **分页加载** | 目录子项超过单页（默认 200）时，滚动到底部自动加载下一页（cursor-based） |
| B-4 | **面包屑导航** | 显示当前路径的面包屑，点击任意层级可跳转 |
| B-5 | **树形侧边栏** | 可折叠的左侧目录树，只展示文件夹，点击切换主面板目录 |
| B-6 | **前进/后退** | 维护导航历史栈，支持前进/后退按钮和浏览器快捷键 |
| B-7 | **路径输入** | 点击面包屑可切换为文本输入框，直接输入路径跳转 |
| B-8 | **List / Grid 切换** | 列表视图（详情列）和网格视图（图标缩略图）之间切换 |
| B-9 | **列排序** | List view 下点击列头（名称 / 大小 / 类型 / 修改时间）排序 |
| B-10 | **搜索过滤** | 工具栏搜索框，前端过滤当前目录下已加载的项目名称 |

### 3.2 文件操作 — 读取

| # | 用例 | 描述 |
|---|------|------|
| R-1 | **文件详情面板** | 选中文件后在侧边或底部显示元数据：名称、大小、内容类型、CAS hash、创建时间 |
| R-2 | **文件预览** | 双击文件打开预览面板。内置支持：图片（img tag）、文本/代码（Monaco/highlight）、PDF（嵌入 viewer）、音视频（HTML5 player）。可通过 `renderFilePreview` 自定义 |
| R-3 | **文件下载** | 工具栏按钮 / 右键菜单 / 快捷键下载选中文件。调用 `fs/read` 获取 blob 并触发浏览器下载 |
| R-4 | **批量下载** | 选中多个文件时，逐个下载或打包为 zip（前端 JSZip）|

### 3.3 文件操作 — 写入

| # | 用例 | 描述 |
|---|------|------|
| W-1 | **创建文件夹** | 工具栏 "新建文件夹" 按钮 → 弹出命名对话框 → `fs/mkdir` |
| W-2 | **上传文件** | 工具栏 "上传" 按钮 → 打开文件选择器 → `fs/write` 逐一上传。支持多文件 |
| W-3 | **拖拽上传** | 从桌面拖拽文件/文件夹到浏览区域触发上传，含 drop zone 视觉反馈 |
| W-4 | **上传进度** | 显示每个文件的上传状态（等待中 / 上传中 / 成功 / 失败）和整体进度条 |
| W-5 | **大小限制提示** | 超过 4MB 的文件给出明确错误提示（当前 single-block 限制） |
| W-6 | **重名冲突处理** | 上传/移动时目标已存在同名文件：提示覆盖 / 重命名 / 跳过 |
| W-7 | **重命名** | 选中文件或文件夹 → F2 / 右键"重命名" → 内联编辑 → `fs/mv` |
| W-8 | **删除** | 选中项 → Delete 键 / 右键"删除" → 确认对话框 → `fs/rm`。支持批量 |
| W-9 | **移动** | 拖拽文件到目标文件夹 / 剪切+粘贴 → `fs/mv` |
| W-10 | **复制** | Ctrl+C / 右键"复制" → Ctrl+V / 右键"粘贴" → `fs/cp`（CAS 零拷贝）|
| W-11 | **原子批量操作** | 多个操作合并为一次 `fs/rewrite` 调用（最多 100 entries），减少 commit 次数 |

### 3.4 选择与交互

| # | 用例 | 描述 |
|---|------|------|
| S-1 | **单选** | 点击选中单个项 |
| S-2 | **多选** | Ctrl+Click 切换选中，Shift+Click 范围选中 |
| S-3 | **全选** | Ctrl+A 选中当前目录所有项 |
| S-4 | **右键菜单** | 右键弹出上下文菜单，根据选中项类型和权限动态生成菜单项 |
| S-5 | **拖拽移动** | 在树内拖拽文件/文件夹到目标目录 |
| S-6 | **拖拽排序反馈** | 拖拽时高亮有效 drop target，无效目标显示禁止图标 |
| S-7 | **键盘导航** | ↑↓ 选择、Enter 打开、Backspace 上级、Delete 删除、F2 重命名 |

### 3.5 CAS 特有功能

| # | 用例 | 描述 |
|---|------|------|
| C-1 | **显示 CAS Hash** | 文件详情面板显示节点的 `nod_XXXX` 标识符 |
| C-2 | **CAS URI 复制** | 右键 "复制 CAS URI" → 生成 `nod_XXXX/path#index` 格式的 URI 到剪贴板 |
| C-3 | **Root 追踪** | 每次写操作后显示新 root hash，状态栏显示当前 depot root |
| C-3.5 | **Depot 选择器** | 未指定 `depotId` 时，组件首屏显示 depot 列表（卡片/列表），用户选择后进入文件浏览。支持搜索、显示 depot 名称和 ID |
| C-4 | **Depot 历史** | 查看当前 depot 的 root history（最近 N 次 commit），支持回滚到历史版本浏览 |
| C-5 | **Content 去重指示** | 对于 CAS 相同 hash 的文件，在 UI 上标注（例如复制后的文件显示 "零拷贝" badge） |
| C-6 | **Index Path 透传** | 读取操作自动附加 `X-CAS-Index-Path` 头，对开发者透明 |

### 3.6 权限与安全

| # | 用例 | 描述 |
|---|------|------|
| P-1 | **只读模式** | delegate 无 `canUpload` 时，所有写操作 UI 自动隐藏/禁用 |
| P-2 | **Scope 限制** | delegate 的 scope 限定了可访问的子树，超出范围的节点不可访问，导航时友好提示 |
| P-3 | **Token 自动刷新** | Access Token 过期时自动通过 Refresh Token 续期，对用户透明 |
| P-4 | **Token 过期处理** | Refresh Token 也过期时，触发 `onError` 回调（type: "auth_expired"），宿主应用决定如何处理（如重新登录） |
| P-5 | **操作粒度权限** | 写操作失败（403）时显示明确的权限不足提示 |

### 3.7 用户体验

| # | 用例 | 描述 |
|---|------|------|
| U-1 | **加载态** | 目录加载、文件操作中显示 skeleton / spinner |
| U-2 | **空目录状态** | 空文件夹显示引导文案和"上传文件"/"新建文件夹"快捷入口 |
| U-3 | **错误处理** | 网络错误、权限错误、文件过大等统一的错误 Snackbar 提示 |
| U-4 | **操作确认** | 删除等危险操作弹出确认对话框 |
| U-5 | **撤销提示** | 删除后显示 "已删除 X 项" Snackbar 并附带"撤销"按钮（在 commit 前可恢复） |
| U-6 | **响应式布局** | 适配桌面和平板宽度。移动端隐藏侧边栏，工具栏折叠为 overflow menu |
| U-7 | **暗色模式** | 跟随 MUI theme 的 `palette.mode` |
| U-8 | **无障碍** | ARIA 标签、键盘焦点管理、屏幕阅读器支持 |
| U-9 | **国际化** | 所有 UI 文案可通过 `messages` prop 覆盖，内置 en-US / zh-CN |

---

## 4. UI 布局

```
┌─────────────────────────────────────────────────────────────────┐
│ Toolbar                                                         │
│ [← →] [↑] 📁 Home / Documents / Project    🔍 [⊞][≡] [⚙]    │
├────────────────────┬────────────────────────────────────────────┤
│ Tree Sidebar       │ Main Panel                                 │
│                    │                                            │
│ 📁 Home           │  Name          Size    Type    Modified    │
│  ├─ 📁 Documents  │  📁 src        —      Folder   2h ago     │
│  │  ├─ 📁 Project │  📁 docs       —      Folder   1d ago     │
│  │  └─ 📁 Archive │  📄 README.md  2.1KB  text/md  3h ago     │
│  ├─ 📁 Pictures   │  🖼 logo.png  48KB   image    5d ago     │
│  └─ 📁 Music      │                                            │
│                    ├────────────────────────────────────────────│
│                    │ Detail / Preview Panel (collapsible)       │
│                    │ Name: README.md                            │
│                    │ Size: 2.1 KB                               │
│                    │ Hash: nod_3FG7K...                         │
│                    │ Content-Type: text/markdown                 │
├────────────────────┴────────────────────────────────────────────┤
│ Status Bar: Root: nod_7X2M... │ 142 items │ 3 selected         │
└─────────────────────────────────────────────────────────────────┘
```

### 4.1 区域说明

| 区域 | 内容 |
|------|------|
| **Toolbar** | 导航按钮（后退/前进/上级）、面包屑、搜索框、视图切换（List/Grid）、操作按钮（新建/上传/下载/删除）、设置 |
| **Tree Sidebar** | 可折叠。仅显示文件夹的树形结构，懒加载。点击切换主面板。当前目录高亮 |
| **Main Panel** | List view: 可排序数据表格。Grid view: 图标/缩略图卡片网格。支持虚拟滚动 |
| **Detail Panel** | 可折叠。显示选中项元数据或文件预览 |
| **Status Bar** | 当前 depot root hash、目录项数统计、选中数量 |

---

## 5. 键盘快捷键

| 快捷键 | 操作 |
|--------|------|
| `↑` / `↓` | 上下移动焦点 |
| `Enter` | 打开文件/进入文件夹 |
| `Backspace` | 返回上级目录 |
| `Delete` | 删除选中项 |
| `F2` | 重命名 |
| `Ctrl+C` | 复制 |
| `Ctrl+X` | 剪切 |
| `Ctrl+V` | 粘贴 |
| `Ctrl+A` | 全选 |
| `Ctrl+D` | 下载 |
| `Ctrl+Shift+N` | 新建文件夹 |
| `Ctrl+U` | 上传文件 |
| `Ctrl+F` | 聚焦搜索框 |
| `Ctrl+Shift+1` | 切换列表视图 |
| `Ctrl+Shift+2` | 切换网格视图 |
| `Esc` | 取消选择 / 关闭面板 |
| `F5` | 刷新当前目录 |

---

## 6. 右键菜单

### 6.1 文件右键

```
┌────────────────────────┐
│ 📋 打开                │
│ 📋 在新标签页打开预览   │
│ ──────────────────── │
│ ✂️ 剪切          Ctrl+X │
│ 📋 复制          Ctrl+C │
│ ──────────────────── │
│ ✏️ 重命名         F2    │
│ 🗑 删除          Delete │
│ ──────────────────── │
│ ⬇️ 下载          Ctrl+D │
│ 🔗 复制 CAS URI        │
│ ℹ️ 属性                │
└────────────────────────┘
```

### 6.2 文件夹右键

```
┌────────────────────────┐
│ 📂 打开                │
│ ──────────────────── │
│ ✂️ 剪切          Ctrl+X │
│ 📋 复制          Ctrl+C │
│ 📋 粘贴          Ctrl+V │  ← 仅剪贴板有内容时显示
│ ──────────────────── │
│ ✏️ 重命名         F2    │
│ 🗑 删除          Delete │
│ ──────────────────── │
│ 📁 新建文件夹          │
│ ⬆️ 上传到此目录         │
│ 🔗 复制 CAS URI        │
│ ℹ️ 属性                │
└────────────────────────┘
```

### 6.3 空白区域右键

```
┌────────────────────────┐
│ 📋 粘贴          Ctrl+V │
│ ──────────────────── │
│ 📁 新建文件夹          │
│ ⬆️ 上传文件            │
│ ──────────────────── │
│ 🔄 刷新           F5   │
│ ℹ️ 文件夹属性          │
└────────────────────────┘
```

> 所有写操作菜单项在只读模式下自动隐藏。自定义菜单项通过 `extraContextMenuItems` 注入。

---

## 7. 错误场景

| 场景 | 处理方式 |
|------|---------|
| 网络断开 | Snackbar 提示 "网络不可用"，操作排队等待恢复 |
| Access Token 过期 | 自动通过 RT 刷新，无感知 |
| Refresh Token 过期 | 触发 `onError({ type: "auth_expired" })`，暂停操作 |
| 403 权限不足 | Snackbar 提示具体缺少的权限 |
| 404 路径不存在 | 自动跳转到最近的有效父目录 |
| 文件过大（>4MB） | 选择文件时立即提示，不发送请求 |
| 文件名非法 | 输入时实时校验，显示行内错误提示 |
| 名称冲突 | 弹出对话框：覆盖 / 重命名 / 跳过 |
| 目录子项超限（>10,000）| 提示 "此目录包含过多子项" |
| 并发冲突（root 已变更）| 提示 "目录已被其他操作修改"，提供刷新选项 |

---

## 8. 技术选型

| 层面 | 选择 | 理由 |
|------|------|------|
| UI 框架 | MUI v6 (`@mui/material`) | 项目已使用，一致性 |
| 状态管理 | Zustand | 轻量、项目已使用 |
| 虚拟滚动 | `@tanstack/react-virtual` | 高性能，list & grid 统一方案 |
| 拖拽 | `@dnd-kit/core` | 现代 React DnD 库，支持可访问性 |
| 树形视图 | `react-arborist` 或自建 | 侧边栏树组件 |
| 图标 | `@mui/icons-material` | MUI 配套 |
| 国际化 | 内置简单 i18n（messages 对象） | 不引入额外 i18n 框架 |
| 打包 | tsup | 项目统一 |

### peerDependencies

```json
{
  "react": "^18.0.0 || ^19.0.0",
  "@mui/material": "^6.0.0",
  "@casfa/client": "workspace:*"
}
```

---

## 9. 组件分解

```
<CasfaExplorer>                    # 顶层容器，提供 Context
├── <DepotSelector>                # depot 列表（depotId 未指定时显示）
├── <ExplorerToolbar>              # 工具栏
│   ├── <NavigationButtons>        # ← → ↑
│   ├── <Breadcrumb>               # 面包屑 / 路径输入
│   ├── <SearchBox>                # 搜索过滤
│   ├── <ViewToggle>               # List / Grid 切换
│   └── <ActionButtons>            # 新建、上传、下载、删除
├── <ExplorerBody>                 # 主体区域 (flex row)
│   ├── <TreeSidebar>              # 可折叠文件夹树
│   ├── <FileList>                 # 列表视图 (MUI DataGrid 或自建虚拟表格)
│   ├── <FileGrid>                 # 网格视图 (虚拟网格)
│   └── <DetailPanel>              # 详情/预览面板
├── <ContextMenu>                  # 右键菜单 (MUI Menu)
├── <UploadOverlay>                # 拖拽上传覆盖层
├── <UploadProgress>               # 上传进度面板
├── <ConfirmDialog>                # 确认对话框（删除等）
├── <RenameDialog>                 # 重命名对话框
├── <CreateFolderDialog>           # 新建文件夹对话框
├── <ConflictDialog>               # 名称冲突处理对话框
├── <StatusBar>                    # 底部状态栏
└── <ErrorSnackbar>                # 全局错误提示
```

---

## 10. Hooks API（Headless 使用）

除了完整 UI 组件外，导出核心 hooks 供自定义 UI：

```ts
// Depot 列表
useDepots(client)                  → { depots, isLoading, refresh }

// 目录浏览
useDirectory(path)                 → { items, isLoading, hasMore, loadMore, refresh }

// 树形数据
useDirectoryTree(rootPath)         → { tree, expandNode, collapseNode }

// 文件操作
useFileOperations()                → { upload, download, mkdir, rm, mv, cp, rename }

// 选择
useSelection()                     → { selected, select, deselect, toggleSelect, selectAll, clearSelection }

// 剪贴板
useClipboard()                     → { clipboard, cut, copy, paste, canPaste }

// 导航
useNavigation()                    → { currentPath, navigate, goBack, goForward, goUp, canGoBack, canGoForward }

// 搜索
useSearch()                        → { searchTerm, setSearchTerm, filteredItems }

// 上传队列
useUploadQueue()                   → { queue, addFiles, cancelUpload, retryUpload, progress }
```

---

## 11. 与 `@casfa/client` 的集成

### 11.1 需要新增的 Client 方法

当前 `@casfa/client` 没有 `fs.*` 方法，组件需要以下 API：

```ts
interface CasfaClientFs {
  stat(depotId: string, path: string): Promise<FsStatResult>;
  ls(depotId: string, path: string, opts?: { limit?: number; cursor?: string }): Promise<FsLsResult>;
  read(depotId: string, path: string): Promise<{ blob: Blob; contentType: string }>;
  write(depotId: string, path: string, data: Blob | ArrayBuffer): Promise<{ newRoot: string }>;
  mkdir(depotId: string, path: string): Promise<{ newRoot: string }>;
  rm(depotId: string, path: string): Promise<{ newRoot: string }>;
  mv(depotId: string, src: string, dst: string): Promise<{ newRoot: string }>;
  cp(depotId: string, src: string, dst: string): Promise<{ newRoot: string }>;
  rewrite(depotId: string, entries: RewriteEntry[], deletes?: string[]): Promise<{ newRoot: string }>;
  commitRoot(depotId: string, newRoot: string): Promise<void>;
}
```

### 11.2 Token 管理

**组件不负责 token 管理**。`CasfaClient` 已内置自动刷新机制：
1. Client 拦截 401 响应
2. 使用 RT 调用 `/tokens/refresh`
3. 获取新 AT 后重试原始请求
4. RT 也过期时，组件通过 client 的错误回调捕获并触发 `onError({ type: "auth_expired" })`

组件只是 client 的消费者，不创建也不销毁 client。宿主应用全权管理 client 生命周期。

---

## 12. 实现优先级

### P0 — MVP（第一阶段）

- [ ] 目录浏览（ls + 懒加载）
- [ ] 面包屑导航
- [ ] List view（名称、大小、类型列）
- [ ] 文件下载
- [ ] 文件上传（按钮 + 拖拽）
- [ ] 创建文件夹
- [ ] 删除（单个 + 批量）
- [ ] 重命名
- [ ] 基本右键菜单
- [ ] 权限感知（只读模式）
- [ ] Depot 选择器（depotId 未指定时）
- [ ] 错误处理 & Snackbar

### P1 — 增强

- [ ] 树形侧边栏
- [ ] Grid view
- [ ] 文件预览面板
- [ ] 复制/移动（剪贴板）
- [ ] 键盘快捷键
- [ ] 搜索过滤
- [ ] 列排序
- [ ] 上传进度面板
- [ ] CAS Hash 显示
- [ ] 状态栏

### P2 — 高级

- [ ] CAS URI 复制
- [ ] Depot 历史/回滚
- [ ] 批量操作（rewrite）
- [ ] 去重指示
- [ ] Detail panel（完整元数据）
- [ ] 响应式/移动端适配
- [ ] 暗色模式
- [ ] 国际化（zh-CN）
- [ ] Headless hooks 导出
- [ ] 自定义列/菜单/工具栏扩展点

---

## 13. 待确认问题

1. **大文件上传**：当前 single-block 限制 4MB，是否需要在组件层支持 multi-block（s-node 链接）上传？还是先限制 4MB，等 server 支持后再扩展？
2. **实时协作**：多个用户同时操作同一 depot 时，是否需要 WebSocket 推送更新？还是手动刷新？
3. ~~**Depot 选择器**~~：✅ 已确认 — `depotId` 可选，省略时组件内置 depot 选择器。
4. **离线支持**：是否需要本地缓存目录结构以支持离线浏览？
5. **文件编辑**：是否需要在组件内集成文本编辑器（如 Monaco），支持直接编辑并保存？
