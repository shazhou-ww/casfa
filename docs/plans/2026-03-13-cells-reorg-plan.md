# Cells Reorganization Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Move all Cells from `apps/` to `cells/`, and rename `server-next` to `drive` and `image-workshop` to `artist` across package/cell/stack/mount wiring.

**Architecture:** This is a structural refactor with no functional behavior change. We migrate directories first, then update package identities and configuration wiring, and finally run focused verification to confirm workspace resolution, config loading, and test stability.

**Tech Stack:** Bun workspaces, YAML stack/cell configs, Otavia config loader/tests, TypeScript.

---

### Task 1: Add `cells/*` workspace support

**Files:**
- Modify: `package.json`

**Step 1: Update workspace globs**
- Add `cells/*` to root `workspaces`.
- Keep existing `apps/*` for non-cell apps.

**Step 2: Update root scripts that hardcode old cell paths**
- Replace direct references to `apps/server-next` with `cells/drive` where applicable.

**Step 3: Verify syntax**
- Run: `bun run lint` (or targeted check if full lint is expensive)
- Expected: no JSON/script syntax issues.

### Task 2: Move directories to `cells/` and apply renames

**Files:**
- Move: `apps/sso` -> `cells/sso`
- Move: `apps/agent` -> `cells/agent`
- Move+Rename: `apps/server-next` -> `cells/drive`
- Move+Rename: `apps/image-workshop` -> `cells/artist`

**Step 1: Perform directory moves**
- Use shell move commands from repo root.

**Step 2: Confirm moved tree**
- Verify each destination exists and source no longer exists.

### Task 3: Rename package identities and cell names

**Files:**
- Modify: `cells/drive/package.json`, `cells/artist/package.json`
- Modify: `cells/drive/cell.yaml`, `cells/artist/cell.yaml`

**Step 1: Rename package names**
- `@casfa/server-next` -> `@casfa/drive`
- `@casfa/image-workshop` -> `@casfa/artist`

**Step 2: Rename cell names**
- `casfa-next` -> `drive`
- `image-workshop` -> `artist`

### Task 4: Update stack and app wiring

**Files:**
- Modify: `stack.yaml`
- Modify: `apps/main/otavia.yaml`
- Modify: `apps/main/package.json`

**Step 1: Update stack cell list**
- Replace `server-next` with `drive`
- Replace `image-workshop` with `artist`

**Step 2: Update otavia cell packages and mounts**
- `@casfa/server-next` -> `@casfa/drive`
- `@casfa/image-workshop` -> `@casfa/artist`
- mount `workshop` -> `artist`
- keep mount `drive` as drive

**Step 3: Update main package deps**
- Replace old package deps with new package names.

### Task 5: Update runtime/config/test references to moved paths and renamed packages

**Files:**
- Modify: `apps/otavia/src/config/resolve-cell-dir.ts`
- Modify: `apps/otavia/src/config/__tests__/load-otavia-yaml.test.ts`
- Modify: `apps/cell-cli/src/mcp-server.ts`
- Modify: `apps/cell-cli/src/config/stack-yaml-schema.ts` (if it hardcodes known examples)
- Modify: any non-doc runtime/test file matching old names/paths

**Step 1: Update fallback directory resolution**
- Ensure fallback targets `cells/<slug>` instead of `apps/<slug>`.

**Step 2: Update tests for new package names/mounts**
- Replace old package expectations and mount labels where required.

**Step 3: Update instructional/example strings**
- Update non-doc examples used by tooling to point to `cells/`.

### Task 6: Update lockfile and verify

**Files:**
- Modify: `bun.lock` (auto-generated by Bun)

**Step 1: Regenerate dependency graph metadata**
- Run: `bun install --no-cache`
- Expected: lockfile aligns with moved workspace paths and package renames.

**Step 2: Run targeted verification**
- Run relevant tests around otavia config and routing.
- Run lints for edited files.

**Step 3: Final grep sweep**
- Ensure old package names and old `apps/<cell>` paths do not remain in runtime/build files (excluding docs).
