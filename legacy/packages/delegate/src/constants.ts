/**
 * Delegate entity constants
 */

/**
 * Maximum delegation depth (0-15).
 * Root delegate is depth 0, max child depth is 15.
 */
export const MAX_DEPTH = 15;

/**
 * Root delegate depth
 */
export const ROOT_DEPTH = 0;
